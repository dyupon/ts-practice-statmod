\section{ћетоды стабилизации дисперсии в разных модел€х}

ѕредположим, что  $\mathsf{X}_n$ --- временной р€д с $\mathbb{E}\mathsf{X}_n = \mu$ and $\mathbb{D}\mathsf{X}_n = h(\mu)$. Ќаша цель --- получить временной р€д $\mathsf{Y}_n = g(x_n)$, где $\mathbb{D}\mathsf{Y}_n$ будет (приблизительно) посто€нен во времени.
 

ћы можем аппроксимировать $\mathsf{Y}_n$ через приближение “ейлора первого пор€дка:

\[ \mathsf{Y}_n = g(x_n) \sim g(\mu) + g^{\prime}(\mu)(x_n - \mu).\]

ѕосмотрим на среднее и дисперсию $\mathsf{Y}_n$:
\[
\mathbb{E}\mathsf{Y}_n = g(\mu) \\
\mathbb{D}\mathsf{Y}_n = h(\mu)g^{\prime}(\mu)^2
\]

»з этого можно сделать вывод, что дл€ стабилизации дисперсии необходимо найти преобразование $g$ такое, что $h(\mu)g^{\prime}(\mu)^2 = \text{const}$ или хот€ бы приблизительно константа.

≈сли $h(\mu)g^{\prime}(\mu)^2 = c$, и $g(\mu) = \log\mu$, то $h(\mu) = c\mu^2$. Ёто выполн€етс€, если модель мультипликативна (\emph{$\log$--преобразование}), в р€дах с такой моделью $\mathsf{Y} = \log\mathsf{X}$.


≈сли $h(\mu)g^{\prime}(\mu)^2 = c$, и $g(\mu) = \sqrt\mu$, то $h(\mu) = 4c\mu$. Ёто \emph{sqrt--преобразование}, имеет смысл, когда шум пуассоновский, в таком случае: $\mathsf{Y} = \sqrt{\mathsf{X}}$.

≈сли данные от $0$ до $1$ (веро€тности, например): $h(\mu) = \sqrt{\mu(1 - \mu)}, g(\mu) = \arcsin(\mu).$ “огда $\mathsf{Y} = \arcsin\mathsf{X}$.