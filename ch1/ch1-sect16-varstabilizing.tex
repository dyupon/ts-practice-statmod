\section{Методы стабилизации дисперсии в разных моделях}

Для мультипликативной модели: $\mathsf{Y} = \log\mathsf{X}$.

Если шум пуассоновский: $\mathsf{Y} = \sqrt{\mathsf{X}}$.

Если данные от $0$ до $1$ (вероятности, например): $\mathsf{Y} = \arcsin\mathsf{X}$.