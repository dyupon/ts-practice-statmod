\section{ѕоследовательный SSA}

ѕусть $\SSA (\tsX, L, I = \{I_1, \ldots, I_r\})$ --- разложение временного р€да $\tsX$ методом SSA с длиной окна $L$ на $r$ временных р€дов с группировкой компонент $I$. «аметим, что метод SSA можно примен€ть к р€ду последовательно, например, применив метод SSA с одной длиной окна выделить конкретную компоненту р€да, а затем применить метод SSA с другой длиной окна к остатку:
\begin{equation*}
\SSA (\tsX, L_1, \{I_1, I_2\}) = \{\tsX_1, \tsX_2\}, \ \SSA (\tsX_2, L_2, I = \{I_{21}, \ldots, I_{2r}\}) = \{\tsX_{21}, \ldots, \tsX_{2r}\}.
\end{equation*}

ƒанна€ техника называетс€ Sequential SSA. ќсновное применение этого метода следующее: выделить тренд, применив SSA с маленькой (но не слишком) длиной окна, кратной периоду р€да и последующим применением SSA с длиной окна, близкой к половине длины р€да, дл€ выделени€ сезонности и разложени€ еЄ на отдельные периоды.