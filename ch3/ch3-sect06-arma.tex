\section{ARMA(p,q)}

ћоделью $\text{ARMA}(p, q)$, где $p$ и $q$ Ч целые числа, задающие пор€док модели, называетс€ следующий процесс

\[ x_n = c + \varepsilon_n + \sum_{i=1}^{p}\phi_i x_{n - i} + \sum_{i=1}^{q}\theta_i\varepsilon_{n - i}. \]

«апись через оператор сдвига
\[\Phi_p(L)x_n = \Theta_q(L)\varepsilon_n. \]

«аметим, что дл€ того, чтобы процесс ARMA(p, q) был реализуем относительно шума $\varepsilon_n$ (то есть чтобы выполн€лись услови€ стационарности) необходимо и достаточно, чтобы все корни характеристического полинома лежали вне единичной окружности.

≈сли мы имеем дело со смешанной моделью $\text{ARMA}(p, q)$, то графики уже не помогут. ƒл€ того, чтобы определить $p$ и $q$, мы можем воспользоватьс€ информационными критери€ми. ќбозначим $\theta = (\phi_1, \phi_2,\ldots,\phi_p,\theta_1,\theta_2,\ldots,\theta_q)$ --- параметры модели, $\tsX_N$ --- р€д, $L = L(\theta|\tsX_N)$ Ц-- максимум функции правдоподоби€ дл€ модели со степен€ми $p$ и $q$. ћы рассмотрим несколько информационных критериев:
\begin{align*}
\text{AIC} = -2\log L + 2 (p + q + 1), \\
\text{AIC}_c = \text{AIC} + \displaystyle\frac{2(p+q+k+1)(p+q+k+2)}{N-p-q-k-2}, \\
\text{BIC} = \text{AIC} + \lfloor \log N - 2\rfloor (p+q+k+1).
\end{align*}


ћы выбираем какой-нибудь из критериев, перебираем несколько пар значений $(p, q)$ и останавливаемс€ на той модели, дл€ которой значение выбранного критери€ наименьшее.